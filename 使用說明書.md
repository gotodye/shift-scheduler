# 排班系統 使用說明書

網址：https://gotodye.github.io/shift-scheduler/

---

## 0. 語言

右上角**使用者選單**（或登入畫面）的語言選單可切換 **中文 / English / Tiếng Việt / ไทย**，選擇會記住。介面文字、星期、比對判定都會跟著翻譯。

## 0.5 加入主畫面（當成 App 用）

可以把系統「安裝」到手機主畫面，開起來像原生 App（全螢幕、沒有網址列）。

**iPhone（Safari）**
1. 用 **Safari** 開啟網址。
2. 點下方**分享**鈕 → **加入主畫面**。
3. 主畫面會出現「排班系統」圖示，點開即全螢幕使用。

> iOS 只有用 Safari 加入才會全螢幕（其他瀏覽器不支援）。Android 用 Chrome 選單的「安裝應用程式／加到主畫面」即可。

## 1. 登入與權限

- 用**公司授權的 Google 帳號**登入。沒被授權的帳號登入後會看到「沒有存取權限」。
- 角色分兩種：
  - **管理員**：可看／編所有單位，能管理使用者（白名單）、看獎金報表與編輯紀錄。`gotodye@gmail.com` 是預設管理員。
  - **一般使用者**：只看得到、也只能編輯被指派的單位。
- 資料存在雲端，多人**即時同步**；每段班別各自獨立儲存，多人同時排也不會互相蓋掉。

## 2. 設定白名單（新增/管理使用者）— 管理員限定

1. 右上角**使用者選單** → **「使用者」**。
2. 下方輸入對方的 **Google Email**。
3. 選擇權限：
   - 勾 **「設為管理員」** → 對方能管理所有單位與使用者；或
   - 勾選 **可編輯的單位**（可複選，如泰國、越南）。
4. 按 **「新增 / 更新」**。清單會即時更新。
5. 要調整某人權限：重新輸入同一個 Email、重選單位、再按「新增 / 更新」即可覆蓋。
6. 要移除：清單右側點 **「移除」**。

> 對方之後用自己的 Google 帳號登入，就只會看到你指派的單位。

## 3. 介面總覽

- **導覽列**（電腦在左側、手機在底部）：**排班 / 週檢視 / 月班表 / 統計 / 比對 / 獎金（管理員）**。
  - 電腦版：滑鼠移到左側欄會展開顯示文字；目前所在的頁面會**高亮**。
  - 手機版：底部一排圖示，點了就切換。
- **上方列**：日期切換（‹ ›、今天、**點日期開日曆**選日）、**匯出打卡檔**、**使用者選單**（主題深淺色／語言／使用者／編輯紀錄／登出）。
- **單位分頁**：印尼／越南／泰國／菲律賓／客戶審查（只顯示你有權的；手機可左右滑動）。
- **排班工具**（工具列）：班別範本、筆刷（上班／休假）、套用範本到全組、新增人員、更多（管理範本／複製某日／複製上週／交換移動／清空當日）。
  - **手機版工具列預設收合**，點 **「排班工具 ›」** 展開，保持畫面清爽。
- **排班表**：左邊是人員，上面是時間（一天 48 格、每格半小時），紅線是目前時間。

## 4. 排班操作

> **電腦用拖曳、手機用點擊** —— 手機拖曳會和左右捲動衝突，所以改為點擊建立，更順手。

### 建立 / 調整班別

**電腦版**
- **建立**：在某人時間軸的空白處**按住拖曳** → 放開，畫出一段班。
- **調動**：拖曳整條班左右移動。
- **改上下班**：拉班的左右兩端。
- **編輯 / 刪除 / 改類型**：**點該班別** → 視窗可改開始/結束、切換「上班↔休假」、或刪除。
- 滑鼠移到班塊上會出現**小卡**，顯示起訖、工時、是否超標。

**手機版**
- **建立**：**點時間軸空白處** → 跳出「新增班別」視窗，直接設定起訖時間，或**點範本快捷**（如「早班 09:00–18:00」）一鍵帶入 → 確定。
- **編輯 / 刪除**：**點該班別** → 同一個視窗調整或刪除。

- 一人一天可有**多段班**（中間空檔匯出時會自動變成休息時間，最多 3 組）。

### 休假 / 不可上班
- **整日休假**：點該人左側的 **「休」**（再按一次取消）。
- **某時段不可上班**：工具列筆刷切到 **「休假」**，在時間軸拖出那段（電腦），或用新增視窗把類型選「休假」（手機）。

### 快速鋪班（每天工時不同時很好用）
- 先點一個**範本**（如「早班 09:00–18:00」）→
  - 按 **「套用範本到全組」**：整組當天一次套上；或
  - 點各人左側 **⤵**：只套給該人。
- **管理範本**（更多 → 管理範本）：新增／刪除常用班別。
- **複製某日**：把某一天的整組班表複製到目前這天（可選只複製本單位或全部單位）。
- **清空當日**：清掉本單位當天所有班。**清空後會跳出「已清空當日 · 復原」提示，6 秒內可按「復原」還原。**

### 整月模式（一次排一個人一整個月）
- 點某人**姓名**，或該列的 **「月」** 鈕 → 進入整月畫面（每天一列）。
- 操作與日檢視相同（電腦拖曳、手機點擊）。
- 上方快速鈕：
  - **套用到整月每天** / **只套用平日（一~五）**（先選範本）
  - **週末設休假**
  - **清空整月**（同樣可**復原**）
  - **編輯人員**（改工號 / 姓名 / 刪除）
- 用 ‹ › 換月份。

## 5. 匯出打卡檔（給阿波羅）

1. 上方 **「匯出打卡檔」** → **預設帶入當月**（可自行改起訖日期）→ 可選「匯出全部單位」或只本單位。
2. 按 **「預覽」** → 下方會**列出即將寫入阿波羅的每一筆**（姓名／日期／打卡時段；休假顯示「休假」）。若該範圍有超標時數，會提示「超標 X 小時（另列於獎金報表）」。
3. 看過內容後按 **「確認匯出」** 下載，直接上傳阿波羅打卡系統。
4. **改了日期或單位會要重新預覽**，確保你確認的就是實際匯出的內容。
5. 轉換規則：
   - 有排班 → 上班（W0001）+ 客服班（B001）+ 上下班時間；中間空檔自動變休息時間。
   - 整日休假 → 休假日（H0001）。
   - 只有部分休假、或完全沒排 → **不輸出該列**（代表需要時可臨時加班）。

## 6. 上傳打卡比對（核對出勤）

1. 導覽列 **「比對」** → 選阿波羅匯出的「上下班紀錄」Excel。
2. 系統用**工號＋日期**自動比對排班，上方以卡片顯示**比對筆數／正常／異常／待確認**，下方逐列列出差異：
   - 遲到 / 早退 / 提早上班 / 延後下班 / 未打卡 / 休假日出勤 / 未排班出勤 / 系統查無此工號。
3. 可設 **「容許誤差(分)」**（預設 5 分內不算遲到／早退）、勾 **「只看異常」**、按 **「匯出差異清單」** 存成 Excel。

## 6.5 勞動條件檢查與獎金時數

系統會依三條規則，把工時自動分成「打卡時數」與「獎金時數」：
- 每天最多 8 小時；連續上班最多 5 天；**外籍學生**每週最多 20 小時（人員設定可勾「外籍學生」）。
- **超出的工時在班塊上以珊瑚紅標示**（滑鼠移上去看原因）。純提醒、不影響排班。

兩份匯出因此分流：
- **匯出打卡檔（阿波羅）**：自動裁成合法範圍（每天最早 8 小時、跳過連續第 6 天、學生每週 ≤20），超出的不打卡。
- **獎金報表（導覽列「獎金」，僅管理員可見）**：輸出超出、不打卡的工時給 HR 發獎金。含明細（實際／打卡／獎金時數＋原因）與每人總計兩個工作表。

> 例：某人某天排 10 小時 → 阿波羅打卡 8 小時，另 2 小時進獎金報表。

## 6.7 工時統計儀表板

導覽列「**統計**」→ 選日期範圍（預設當月）、可選全部單位，顯示：
- **KPI 卡片**：員工數、實際／打卡／獎金總時數、超標天數。
- **超標統計**：超過 8 小時（天）、連續超 5 天（天）、學生週超 20（週）。
- **長條圖**：各員工的打卡＋獎金時數（堆疊）。
- **明細表**：每人上班天數、實際／打卡／獎金時數、超標天數、休假天數（外籍學生會標示）。
- **外籍學生每週時數**：逐週列出，超過 20 小時的週會標紅，方便盯法定上限。
- **跨月趨勢**：近 6 個月的實際／打卡／獎金時數折線圖。
- 可按「匯出統計」存成 Excel。

## 6.8 排班效率工具

- **在班人數（覆蓋）**：日檢視最下面一列顯示每半小時的在班人數；左側可設「最低人力」，不足的時段會標紅。
- **複製上週**（更多）：把上一週的班表整週複製到本週再微調。
- **交換/移動**（更多）：選兩個人＋日期 →「互相交換」兩人當天班別，或「覆蓋」把來源複製給目標。
- **週檢視**（導覽列）：一次看整週每人班別與週合計；點格子可跳到那天編輯。

## 6.9 資料與報表

- **匯入人員**（使用者視窗，管理員）：上傳 Excel 一次建立多位員工。欄位：工號、姓名、單位（代碼或名稱皆可）、外籍學生（選填，填 V 或 是）。重複工號會自動略過。
- **月班表**（導覽列）：整月表格，可切換月份、全部單位。
  - **列印**：橫向 A4、含頁首（標題／月份／單位／列印日期）與頁尾，表頭每頁重複。
  - **匯出**：存成 Excel。

## 6.95 編輯紀錄（誰改了什麼）— 管理員限定

管理員可查看誰在何時做了哪些變更，方便多人共用時追蹤。

- **開啟**：右上**使用者選單 → 編輯紀錄**。
- **記錄內容**：新增／修改／刪除班別、切換整日休假、清空當日／整月、複製某日／上週、套用範本到全組、新增／編輯／刪除人員。
- **顯示**：一列一筆，例如「Angus 新增班別 Budi · 07/11」＋單位＋時間，**最新在最上**，載入最近 200 筆。

## 7. 小提醒

- 資料即時存雲端，換電腦、換瀏覽器登入同一帳號都看得到。
- **離線也能看**上次載入的資料；恢復連線後，離線期間的變更會自動同步。
- **匯出 / 比對**需要能連網（會載入 Excel 元件）。
- 手機、電腦都好用：**電腦拖曳排班最快，手機點擊建立最穩**。
- 畫面／圖示沒更新時，多半是瀏覽器快取——用無痕視窗或強制重新整理即可。
