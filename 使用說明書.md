# 排班系統 使用說明書

網址：https://gotodye.github.io/shift-scheduler/

---

## 0. 語言

右上角（或登入畫面）的語言選單可切換 **中文 / English / Tiếng Việt / ไทย**，選擇會記住。介面文字、星期、比對判定都會跟著翻譯。

## 1. 登入與權限

- 用**公司授權的 Google 帳號**登入。沒被授權的帳號登入後會看到「沒有存取權限」。
- 角色分兩種：
  - **管理員**：可看／編所有單位，並能管理使用者（白名單）。`gotodye@gmail.com` 是預設管理員。
  - **一般使用者**：只看得到、也只能編輯被指派的單位。
- 資料存在雲端，多人**即時同步**；每段班別各自獨立儲存，多人同時排也不會互相蓋掉。

## 2. 設定白名單（新增/管理使用者）— 管理員限定

1. 右上角點 **「使用者」**。
2. 下方輸入對方的 **Google Email**。
3. 選擇權限：
   - 勾 **「設為管理員」** → 對方能管理所有單位與使用者；或
   - 勾選 **可編輯的單位**（可複選，如泰國、越南）。
4. 按 **「新增 / 更新」**。清單會即時更新。
5. 要調整某人權限：重新輸入同一個 Email、重選單位、再按「新增 / 更新」即可覆蓋。
6. 要移除：清單右側點 **「移除」**。

> 對方之後用自己的 Google 帳號登入，就只會看到你指派的單位。

## 3. 介面總覽

- **上方**：日期切換（‹ › 、今天、日曆）、上傳打卡比對、匯出打卡檔、使用者、登出。
- **單位分頁**：印尼／越南／泰國／菲律賓／客戶審查（只顯示你有權的）。
- **工具列**：班別範本、筆刷（上班／休假）、套用範本到全組、複製某日、新增人員、清空當日。
- **排班表**：左邊是人員，上面是時間（一天 48 格、每格半小時）。

## 4. 排班操作

### 建立 / 調整班別
- **建立**：在某人時間軸的空白處**按住拖曳** → 放開，就畫出一段班。
- **調動**：拖曳整條班左右移動。
- **改上下班**：拉班的左右兩端。
- **精確編輯 / 刪除 / 改類型**：**點該班別** → 跳出視窗可改開始/結束時間、切換「上班↔休假」、或刪除。
- 一人一天可有**多段班**（中間空檔匯出時會自動變成休息時間，最多 3 組）。

### 休假 / 不可上班
- **整日休假**：點該人左側的 **「休」**（再按一次取消）。
- **某時段不可上班**：工具列筆刷切到 **「休假」**，在時間軸拖出那段（琥珀色斜紋）。同樣可拖曳、拉長短、點它編輯。

### 快速鋪班（每天工時不同時很好用）
- 先點一個**範本**（如「早班 09:00–18:00」）→
  - 按 **「套用範本到全組」**：整組當天一次套上；或
  - 點各人左側 **⤵**：只套給該人。
- **管理範本**：新增／刪除常用班別。
- **複製某日**：把某一天的整組班表複製到目前這天（可選只複製本單位或全部單位）。
- **清空當日**：清掉本單位當天所有班。

### 整月模式（一次排一個人一整個月）
- 點某人**姓名**，或該列的 **「月」** 鈕 → 進入整月畫面（每天一列）。
- 操作與日檢視相同（拖拉、休假…）。
- 上方快速鈕：
  - **套用到整月每天** / **只套用平日（一~五）**（先選範本）
  - **週末設休假**
  - **清空整月**
  - **編輯人員**（改工號 / 姓名 / 刪除）
- 用 ‹ › 換月份。

## 5. 匯出打卡檔（給阿波羅）

1. 右上 **「匯出打卡檔」** → 選起訖日期 → 可選「匯出全部單位」或只本單位。
2. 按 **「產生 Excel」**，下載後直接上傳阿波羅打卡系統。
3. 轉換規則：
   - 有排班 → 上班（W0001）+ 客服班（B001）+ 上下班時間；中間空檔自動變休息時間。
   - 整日休假 → 休假日（H0001）。
   - 只有部分休假、或完全沒排 → **不輸出該列**（代表需要時可臨時加班）。

## 6. 上傳打卡比對（核對出勤）

1. 右上 **「上傳打卡比對」** → 選阿波羅匯出的「上下班紀錄」Excel。
2. 系統用**工號＋日期**自動比對排班，列出差異：
   - 遲到 / 早退 / 提早上班 / 延後下班 / 未打卡 / 休假日出勤 / 未排班出勤 / 系統查無此工號。
3. 可設 **「容許誤差(分)」**（預設 5 分內不算遲到／早退）、勾 **「只看異常」**、按 **「匯出差異清單」** 存成 Excel。

## 7. 小提醒

- 資料即時存雲端，換電腦、換瀏覽器登入同一帳號都看得到。
- 匯出 / 比對需要能連網（會載入 Excel 元件）。
- 手機也能開，但排班用電腦寬螢幕最順手。
